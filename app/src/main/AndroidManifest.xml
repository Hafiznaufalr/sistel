<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.hafiznaufalr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Flowchart"
            android:label="@string/flowchart"/>
        <activity android:name=".Soon" />
        <activity
            android:name=".Anggota"
            android:label="@string/anggota" />
        <activity
            android:name=".calc.hitungdaya"
            android:label="@string/hitung_daya" />
        <activity
            android:name=".calc.HitungLoss"
            android:label="@string/hitung_los" />
        <activity
            android:name=".calc.HitungGain"
            android:label="@string/hitung_gain" />
        <activity
            android:name=".CalculatorMenu"
            android:label="@string/calc_menu" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_namee">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>